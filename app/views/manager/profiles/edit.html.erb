<%= render "form", profile: @profile %>

<div class="flex justify-between items-center mb-4">
  <h1 class="font-bold text-4xl">Habilidades</h1>
</div>

<% @profile.skills.each do |skill| %>
  <%= render 'skill_fields', skill: skill %>
<% end %>