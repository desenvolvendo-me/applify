<%= form_for [:manager, @interview_test] do |form| %>
  <% if interview_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(interview_test.errors.count, 'error') %> prohibited this interview_test from being saved:</h2>

      <ul>
        <% interview_test.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :company %>
    <%= form.text_field :company %>
  </div>

  <div class="actions cursor-pointer">
    <%= form.submit %>
  </div>
<% end %>
